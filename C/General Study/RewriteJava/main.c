#include <stdio.h>
#include <string.h>

/*
struct Test
{
    int age;
    char name[100];
};


void setName( char currentName[100], char newName[100] )
{
    strcpy( currentName, newName );
}

int main()
{
    struct Test test;
    test.age = 35;
    setName( test.name, "Anders" );

    printf( "Du är %i år gammal", test.age );

    return 0;
}
 */
